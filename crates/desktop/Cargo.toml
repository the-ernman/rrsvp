[package]
name = "rrsvp-desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
rrsvp-core = { path = "../core" }
eframe = { version = "0.31", default-features = false, features = ["default_fonts", "glow"] }
egui = "0.31"
